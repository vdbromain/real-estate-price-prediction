#Created with pipreqs . to have only the libraries I used in this directory I had to add the scikit-learn because
#didn't use it in the deployment's repo but needed to launch the model in the code !
#With pip freeze > requirements.txt I will take every libraries I installed on my computer !
Flask==2.2.2
numpy==1.24.1
scikit-learn==1.2.1

